{
  "name": "@veridion/plugin-access-control",
  "version": "0.1.0",
  "private": true,
  "description": "Access control vulnerability detection plugin",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --max-warnings 0",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@veridion/scanner-types": "workspace:*",
    "@veridion/shared": "workspace:*",
    "@veridion/logger": "workspace:*"
  },
  "devDependencies": {
    "@veridion/tsconfig": "workspace:*",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
